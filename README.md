# streaming-module

For running the node server,
1. cd into server directory
2. Run npm install (For the first time only)
3. Run node app.js

For running the react app on local host 
1. cd into client/rtsp-streamer
2. Run npm install (For the first time only)
3. Run npm start
